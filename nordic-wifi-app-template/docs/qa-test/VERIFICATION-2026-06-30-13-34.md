# QA Report — Nordic Wi-Fi App Template

## Document Information

| Field | Value |
|-------|-------|
| Project | nordic-wifi-app-template |
| Version | 2026-06-30-13-34 |
| PRD Version | 2026-06-30-13-00 |
| Specs Version | 2026-06-30-13-04 |
| NCS Version | v3.3.0 |
| Status | Pass (static) — hardware validation pending |

---

## Changelog

| Version | Summary |
|---------|---------|
| 2026-06-30-13-34 | Verification of the full P2P-pairing body of work: button pairing + PBC + `GLOBAL_HEAP` fix + GO-capability peer filter + re-entrancy guard, plus the two UX tweaks (board-configurable gesture button, pairing-LED BREATHE). |
---

## Build State

| Item | Value |
|------|-------|
| Uncommitted changes | Yes — 22 files (code + specs + PRD + README + QA), nothing committed; work in `p2p-pairing-ux` worktree |
| Build reflects | Uncommitted on-disk state |
---

## 1. Security Risk

| ID | Risk Class | Finding | Verdict |
|----|-----------|---------|---------|
| SEC-1 | Hardcoded credentials | The fixed WPS PIN `12345678` is **fully removed** from source (switched to PBC); only the pre-existing SoftAP default PSK (a documented developer-template default via `CONFIG_`) remains | ✅ Clean |
| SEC-2 | Debug features in release | `CONFIG_SHELL` is a documented template feature; `CONFIG_WIFI_NM_WPA_SUPPLICANT_LOG_LEVEL_DBG` was reverted (commented) after debugging | ✅ Clean |
| SEC-3 | HTTP / unencrypted endpoint | none in scope | ✅ Clean |
| SEC-4 | Key/token in source | none | ✅ Clean |

**Security result**: ✅ No findings. (Switching GO/GC from a fixed public PIN to PBC removed the only constant secret.)

---

## 2. Code Format

| Verdict | Files with violations |
|---------|-----------------------|
| ✅ Clean | none |

NCS-toolchain clang-format `--dry-run` clean on all changed C/H: `wifi_utils.c/.h`, `net_event_mgmt.c/.h`, `net_event_app.c`, `ux.c`, `messages.h`, `wifi.c`.

---

## 3. Build Results

| Board | Result | Compiler Warnings | FLASH | RAM |
|-------|--------|-------------------|-------|-----|
| nRF7002DK (app core) | ✅ PASS | 0 | 94.04% | 85.67% |
| nRF54LM20DK + nRF7002EB2 | ✅ PASS | 0 | 56.86% | 83.95% |
| nRF5340 Audio DK + nRF7002EK | ✅ PASS | 0 | 93.69% | 85.87% |

> RAM dropped ~6–8% vs. the pre-fix builds because `WIFI_NM_WPA_SUPPLICANT_GLOBAL_HEAP=y` removed the separate 40 KB dedicated supplicant heap (supplicant now shares the system heap). Only expected nRF70 `Experimental symbol` Kconfig notices appeared.

---

## 4. PRD Satisfaction Check (Code Reading)

| FR | Criterion | Code Evidence | Verdict |
|----|-----------|---------------|---------|
| FR-006 | P2P_GO group + WPS PBC window on double-click; stays connectable | `wifi_run_p2p_go_mode()` (group_add + `WIFI_WPS_PBC`, continuous re-arm); `wifi_p2p_go_open_pairing_window()` (double-click → re-arm + 120 s breathe window) | ✅ code / ❓ HW |
| FR-104 | Long-press cycles mode; gesture button board-configurable | `ux.c` long-press → `do_mode_cycle()` (cycle incl. P2P_GC); button gate uses `CONFIG_APP_UX_BUTTON_IDX` | ✅ |
| FR-105 | LED feedback incl. BREATHE during BLE prov **or P2P pairing** | `apply_wifi_state_led()` `APP_WIFI_STATE_PAIRING`→BREATHE; `zego_on_net_event_p2p_pairing()` in net_event_app publishes PAIRING/ resolves on end; GC + GO emit the hook | ✅ code / ❓ HW (breathe timing) |
| FR-106 | Double-click = BLE prov (STA/SoftAP) vs pairing (P2P) | `ux.c` branches on `zego_wifi_get_mode()` | ✅ |
| FR-107 | GC button pairing + NVS reconnect, PBC, GO-capability filter | `wifi_utils.c` GC engine: `pbc --join`, `group_capab` GO-bit filter, re-entrancy guard, NVS `net/p2p_gc_go_mac`, reconnect path | ✅ code / ❓ HW (handshake + power-cycle reconnect) |
| FR-108 | *(removed)* | Kconfig + prefix/exact engine gone | ✅ Removed |
| — | Audio DK gesture button = BTN5 (idx 4) | board conf `CONFIG_APP_UX_BUTTON_IDX=4` | ✅ |

**PRD result**: 5 implemented (code-confirmed), 0 partial, 3 not-visible (HW behaviors → 4.2), 0 mismatch.

---

## 5. Documentation Consistency Audit

| Step | Check | Result | Notes |
|------|-------|--------|-------|
| A | `0-overview.md` PRD Version matches latest PRD Changelog | ✅ | `2026-06-30-13-00` |
| A | All FR/NFR traceable to spec | ✅ | FR-105 → ux-module + network-spec; FR-006/107 → network-spec; FR-104/106 → ux-module |
| B | `CONFIG_ZEGO_APP_SPECS_VERSION` matches `0-overview.md` | ✅ | `2026-06-30-13-04` |
| B | `CONFIG_ZEGO_APP_PRD_VERSION` matches PRD | ✅ | `2026-06-30-13-00` |
| B | Spec modules ↔ code | ✅ | ux-module→`src/modules/ux/`; network/wifi are bricks (own specs, reconciled to PBC) |
| C | README features match PRD | ✅ | UX gesture button (board idx), pairing BREATHE, PBC all reflected |
| C | No stale features in README | ⚠️ | Pre-existing phone-as-GO drift remains (out of scope; flagged previously) |

---

## 6. Resolution of Prior Verification Findings (VERIFICATION-2026-06-29-22-47)

| Prior finding | Status |
|---|---|
| P0 — GO WPS Registrar init failure (pairing blocked) | ✅ **Root-caused + fixed**: dedicated supplicant heap starved `wps_registrar_init()`; `WIFI_NM_WPA_SUPPLICANT_GLOBAL_HEAP=y` resolves it. GO reaches AP-ENABLED on hardware (nRF5340 Audio DK). |
| P1 — listener-context `net_mgmt` call | ✅ Fixed (deferred to P2P workq). |
| P2 — GC peer-selection had no GO filter | ✅ Fixed (`group_capab` GO-bit filter). |
| WPS method PIN vs PBC | ✅ Reconciled to PBC (fixed PIN unsupported on nRF GO). |

---

## Summary

**Overall verdict**: PASS (static) — ready for hardware validation.

| Priority | Finding | Route |
|----------|---------|-------|
| — | No P0/P1 static findings | — |
| P2 | README phone-as-GO drift (pre-existing) | Phase 2 doc cleanup (separate) |
| ❓ | Pairing handshake, power-cycle reconnect, BTN5 gesture, pairing BREATHE | `chsh-sk-ncs-4.2-validation` (2 DKs) |

No P0 findings. Version chain aligned (PRD 2026-06-30-13-00 / Specs 2026-06-30-13-04 / prj.conf matches); clean builds + clang-format on all three boards; the prior P0 GO blocker is resolved. The remaining items are hardware behaviors to confirm in Phase 4.2.
