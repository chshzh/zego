# QA Report — Nordic Wi-Fi App Template

## Document Information

| Field | Value |
|-------|-------|
| Project | nordic-wifi-app-template |
| Version | 2026-07-01-11-23 |
| PRD Version | 2026-07-01-10-50 |
| Specs Version | 2026-07-01-10-54 |
| NCS Version | v3.3.0 |
| Status | Pass with issues (P2 only, none new) |

---

## Changelog

| Version | Summary |
|---------|---------|
| 2026-07-01-11-23 | Initial QA review of the disconnection-handling overhaul: STA reconnect generalized to all boards, LED BLINK-trigger narrowed to "reconnection not possible," P2P_GC auto-pairing at boot with infinite retry. |
---

## Build State

| Item | Value |
|------|-------|
| Uncommitted changes | Yes — see list below |
| Build reflects | Uncommitted on-disk state |

Modified files (`git status --short`):
```
 M ../bricks/led/Kconfig                              (pre-existing, unrelated to this session)
 M ../bricks/network/docs/network-spec.md              (this session)
 M ../bricks/network/src/net_event_mgmt.c              (this session)
 M ../bricks/network/src/net_event_mgmt.h              (this session)
 M ../bricks/network/src/wifi_utils.c                  (this session)
 M ../bricks/network/src/wifi_utils.h                  (this session)
 M ../bricks/wifi/src/wifi.c                            (pre-existing, unrelated to this session)
 M ../bricks/wifi_ble_prov/src/wifi_ble_prov.c          (pre-existing, unrelated to this session)
 M ../bricks/wifi_ble_prov/src/wifi_ble_prov.h          (pre-existing, unrelated to this session)
 M README.md                                            (this session)
 M boards/nrf5340_audio_dk_nrf5340_cpuapp.conf          (pre-existing, unrelated to this session)
 M docs/dev-specs/0-overview.md                         (this session)
 M docs/dev-specs/1-architecture.md                     (this session)
 M docs/dev-specs/ux-module.md                          (this session)
 M docs/pm-prd/PRD.md                                   (this session)
 M prj.conf                                             (this session)
 M src/modules/network/net_event_app.c                  (this session)
 M src/modules/ux/Kconfig                               (pre-existing, unrelated to this session)
 M src/modules/ux/ux.c                                  (pre-existing, unrelated to this session)
```

> Files marked "pre-existing" were already modified on disk before this session started (implementing the prior 2026-06-30-13-04 spec revision — `CONFIG_APP_UX_PAIRING_LED_IDX`, `CONFIG_APP_UX_ERROR_LED_IDX`, etc.). This review only covers the files marked "this session."

---

## 1. Security Risk

| ID | Risk Class | File / Location | Finding | Verdict |
|----|-----------|----------------|---------|---------|
| SEC-1 | Hardcoded credentials | `net_event_mgmt.c`, `wifi_utils.c` (this session's diff) | No literal SSIDs/passwords introduced | ✅ Clean |
| SEC-2 | Debug features in release | `prj.conf` (`CONFIG_SHELL`) | Pre-existing, documented developer-template feature (README Shell commands section) | ✅ Clean |
| SEC-3 | HTTP (unencrypted) endpoint | N/A — project has no HTTP/MQTT client | Not applicable | ✅ Clean |
| SEC-4 | Key / token in source | N/A — no Memfault/cloud integration in this project | Not applicable | ✅ Clean |

**Security result**: ✅ No findings

---

## 2. Code Format

| Verdict | Files with violations |
|---------|-----------------------|
| ✅ Clean (this session's lines) | All 5 changed C/H files, after 1 fix |

Ran `clang-format --dry-run --Werror -style=file:<ncs>/zephyr/.clang-format` on every file touched this session:

```
net_event_mgmt.h                : clean
wifi_utils.h                    : clean
net_event_app.c                 : clean
wifi_utils.c                    : 1 violation found and fixed (own new comment, wifi_utils.c:737)
net_event_mgmt.c                : 5 pre-existing violations (lines 225, 258, 831, 878, 882) —
                                   confirmed via `git diff -U0` hunk boundaries that none of these
                                   lines fall inside this session's changes; they predate this
                                   session (DHCP lease-renewal code and the untouched body of
                                   l3_watchdog_arm()/its #else stub).
```

Violation fixed during this review (wifi_utils.c:737 — comment line wrap):
```c
-static uint8_t pairing_find_cycles; /* empty discovery cycles so far (log only - wraps
-				      * harmlessly; pairing itself never gives up) */
+static uint8_t pairing_find_cycles; /* empty discovery cycles so far (log only) */
```

---

## 3. Build Results

Ran exactly the three commands from README `### Build`:

| Board | Result | Compiler Warnings | FLASH | RAM |
|-------|--------|-------------------|-------|-----|
| nRF54LM20DK + nRF7002EB2 | ✅ PASS | 0 (project) | 56.94% (1131212 B / 1940 KB) | 83.98% (439416 B / 511 KB) |
| nRF7002DK | ✅ PASS | 0 (project) | 94.20% (987732 B / 1 MB) | 85.69% (393104 B / 448 KB) |
| nRF5340 Audio DK + nRF7002EK | ✅ PASS | 0 (project) | 93.85% (984052 B / 1 MB) | 85.89% (394032 B / 448 KB) |

> Kconfig `Experimental symbol` notices (`WIFI_NM_WPA_SUPPLICANT`, `WIFI_CREDENTIALS`, `NET_BUF_VARIABLE_DATA_SIZE`, etc.) appeared on all three builds as expected and are not counted as compiler warnings per skill guidance.

nRF7002DK and nRF5340 Audio DK are within 6% of their 1 MB flash ceiling — unchanged from the prior verified baseline (this session added ~200 bytes of code: one helper function, a few extra branches; no new Kconfig-selected subsystems).

---

## 4. PRD Satisfaction Check (Code Reading)

Focused on the FRs changed this session; FRs untouched this session are confirmed unaffected by inspection.

| FR | Acceptance Criterion | Code Evidence | Verdict |
|----|---------------------|---------------|---------|
| FR-001 | Clean build both boards | See §3 | ✅ Implemented |
| FR-002 | STA shell connect | Unchanged (`wifi connect` shell cmd, `NET_REQUEST_WIFI_CONNECT`) | ✅ Implemented |
| FR-003(1) | `wifi cred add` + power cycle → auto-reconnect | `start_mode_work_handler()` STA case, unchanged path when credentials exist | ✅ Implemented |
| FR-003(2) | Runtime disconnect w/ credentials → auto-retry, all boards, independent of BLE prov | `net_event_mgmt.c`: `NET_EVENT_WIFI_DISCONNECT_RESULT` and `CONNECT_RESULT`-failure branches now reschedule `l3_reconnect_work` directly when `!CONFIG_ZEGO_WIFI_BLE_PROV`; `l3_reconnect_work` decoupled from the unrelated DHCP-timeout Kconfig guard | ✅ Implemented (code); ❓ real-hardware retry timing not yet observed |
| FR-003(3) | LED 0 ROTATEs throughout | `zego_on_net_event_wifi_disconnect(true)` → `APP_WIFI_STATE_CONNECTING` → `ux.c` `led_rotate()` (unmodified, pre-verified mapping) | ✅ Implemented |
| FR-003(4) | Zero stored networks → BLINK, no connect attempt | New `wifi_utils_has_stored_credentials()`; `start_mode_work_handler()` calls it before attempting connect | ✅ Implemented |
| FR-004 | STA via BLE prov | Unchanged | ✅ Implemented |
| FR-005(1–4) | SoftAP up to 3 clients, TODO log format | Unchanged | ✅ Implemented |
| FR-005(5) | Unclean client loss detected within AP inactivity timeout | Pre-existing WPA/hostapd behavior; now documented (comment relocated to the correct function in `net_event_mgmt.c`, added to `net_event_app.c`, `network-spec.md`, PRD, ux-module.md) | ✅ Implemented (doc-only change; behavior pre-existing) |
| FR-006(1–5) | P2P_GO group/pairing window/DK-only | Unchanged | ✅ Implemented |
| FR-006(6) | GO expects 1 client, not enforced | Documented only, by design decision — no code change | ✅ Implemented (informational) |
| FR-006(7) | Same 5-min latency note | Same as FR-005(5) | ✅ Implemented |
| FR-007 | Mode persists | Unchanged | ✅ Implemented |
| FR-008 | Net event hook fires | Unchanged | ✅ Implemented |
| FR-101–104 | Buttons, LEDs, memonitor, mode-cycle | Unchanged | ✅ Implemented |
| FR-105(1) | ROTATE during any retry | See FR-003(3); same mechanism for P2P_GC via `zego_on_net_event_wifi_disconnect(true)` unconditionally in the P2P_GC branch | ✅ Implemented |
| FR-105(2–4) | Solid ON / SOFTAP / BREATHE | Unchanged | ✅ Implemented |
| FR-105(5) | BLINK only when STA has zero networks; P2P_GC never blinks | `will_retry` computed per-mode; P2P_GC branch hardcodes `true` unconditionally — confirmed by reading all 3 call sites (`DISCONNECT_RESULT`, `wifi_p2p_gc_on_disconnect()` path, boot) | ✅ Implemented |
| FR-106 | BLE prov double-click toggle | Unchanged | ✅ Implemented |
| FR-107(1) | No target-GO-MAC Kconfig | Unchanged (already removed) | ✅ Implemented |
| FR-107(2) | No saved GO → auto-start pairing at boot, no button | `wifi_run_p2p_gc_mode()` now calls `p2p_gc_start_pairing_internal()` in the `else` branch (was: log-only) | ✅ Implemented (code); ❓ real-hardware discovery/join timing not yet observed |
| FR-107(3) | Double-click also (re-)starts pairing | Unchanged — same `wifi_p2p_start_pairing()` → `p2p_gc_start_pairing_internal()` path, re-entrancy guard intact | ✅ Implemented |
| FR-107(4–5) | PBC join, static IP, MAC saved | Unchanged | ✅ Implemented |
| FR-107(6) | Reconnect to saved GO after disconnect, retries indefinitely | Unchanged mechanism; `will_retry=true` now also signals LED correctly | ✅ Implemented |
| FR-107(7) | Reconnect after power cycle | Unchanged | ✅ Implemented |
| FR-107(8) | New pairing overwrites saved MAC | Unchanged | ✅ Implemented |
| FR-107(9) | ROTATE while reconnecting, BREATHE during any pairing (auto or button) | Auto-pairing reuses `p2p_gc_start_pairing_internal()`, which already calls `zego_on_net_event_p2p_pairing(true)` → BREATHE — same code path as button-triggered, no new state needed | ✅ Implemented |
| FR-108 | *(retired)* | N/A | — |

Additional implementation-discovered behavior not explicitly in the PRD text but required to satisfy FR-003's intent: `CONNECT_RESULT` failure (auth error, timeout, AP not found) does **not** reliably fire `DISCONNECT_RESULT` (confirmed via existing `wifi_ble_prov.c` comment + code). Without also hooking `CONNECT_RESULT` failure, a STA that never associates in the first place would not retry. Fixed in `l2_wifi_conn_event_handler`'s `CONNECT_RESULT` failure branch; documented in `network-spec.md`.

**PRD result**: 27 implemented, 0 partial, 3 not visible (hardware-only timing confirmations, carried to 4.2), 0 mismatch

---

## 5. Documentation Consistency Audit

| Step | Check | Result | Notes |
|------|-------|--------|-------|
| A | `0-overview.md` PRD Version field matches latest PRD Changelog timestamp | ✅ | Both `2026-07-01-10-50` |
| A | All FR/NFR items in PRD traceable to spec | ✅ | FR-003/105/107 → `network-spec.md` + `ux-module.md`; FR-005/006 → `network-spec.md` |
| B | `CONFIG_ZEGO_APP_SPECS_VERSION` in `prj.conf` matches `0-overview.md` latest Changelog | ✅ | Both `2026-07-01-10-54` |
| B | `CONFIG_ZEGO_APP_PRD_VERSION` in `prj.conf` matches PRD latest Changelog | ✅ | Both `2026-07-01-10-50` |
| B | Spec modules have `src/modules/<name>/` counterparts | ✅ | `ux-module.md` ↔ `src/modules/ux/`; network brick documented externally per project convention (`bricks/network/docs/network-spec.md`, linked from `0-overview.md`'s Spec Index) |
| C | README features match current PRD FR list | ✅ | Added "Resilient Wi-Fi reconnection" and updated "Automatic P2P pairing" / LED bullets to match FR-003/105/107 |
| C | No stale features in README | ✅ | Replaced stale "WPS PIN 12345678" wording (didn't match the implemented PBC mechanism) throughout PRD and README |

---

## 6. Fixes Applied During This Verification

| File | Fix | Severity |
|------|-----|----------|
| `bricks/network/src/wifi_utils.c` | Reworded a comment to resolve a clang-format line-wrap violation | P2 |

---

## Summary

**Overall verdict**: PASS WITH ISSUES (issues are pre-existing P2 only; nothing new or blocking)

| Priority | Finding | Route |
|----------|---------|-------|
| P2 | 5 pre-existing clang-format violations in `net_event_mgmt.c` (lines 225, 258, 831, 878, 882), none introduced this session, none in DHCP/L3-watchdog code this session touched | Next iteration — optional `chsh-sk-ncs-clang-format` cleanup pass |
| ❓ | 3 FR sub-criteria (FR-003(2), FR-107(2), and general LED visual confirmation) verified by code reading only — need real hardware to confirm timing and visible behavior | Carry as priority test cases to `chsh-sk-ncs-4.2-validation` |

No P0 or P1 findings. Proceed to hardware validation.
